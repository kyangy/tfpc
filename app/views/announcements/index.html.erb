<section class="announcement animated zoomIn">
	<div class="container-fluid">
		<div class="row text-center">
			<div class="col-md-12">
				<% @announcements.each do |announcement| %>

					<%= image_tag announcement.image.url, :size => "950x600", :crop => :fill %>

				<% end %>
			</div>
		</div>
	</div>
</section>